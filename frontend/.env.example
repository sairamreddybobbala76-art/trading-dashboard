# Copy this to .env.local for local overrides (never commit .env.local)
# Leave empty in development — Vite proxy handles it automatically.
# In Vercel: set VITE_API_URL to your Render backend URL (no trailing slash).
VITE_API_URL=https://your-app.onrender.com
